{% extends 'base.html.twig' %}

{% block title %}New Contact{% endblock %}

{% block content %}
  <div class="container">
    <h1>Create new contact</h1>

    {{ include('contact/_form.html.twig') }}

    <a href="{{ path('app_contact_index') }}" class="btn btn-primary">Back to Contact List</a>
  </div>
{% endblock %}